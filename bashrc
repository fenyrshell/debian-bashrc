# Main file
base_path=$( cd "$(dirname "${BASH_SOURCE[0]}")" ; pwd -P )

source "$base_path/bash_variables"
source "$base_path/bash_functions"
source "$base_path/bash_prompt"
source "$base_path/bash_aliases"
